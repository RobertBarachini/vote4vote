mutation UpdatePlayingField($id: String!, $elementsClicked: String!) {
	updatePlayingField(
		options:{
			id: $id,
			elementsClicked: $elementsClicked
		}
	) {
		_id,
		title,
		updatedAt,
		playingFieldText
	}
}